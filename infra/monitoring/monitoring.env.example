# 이 파일에는 운영 시크릿을 넣지 않는다.
# 서버에서는 SSM Parameter Store 값을 받아 .env.monitoring 파일을 별도로 만든다.
MONITORING_DOMAIN=monitoring.groupware.example.com
GRAFANA_ADMIN_USER=groupware-grafana
GRAFANA_ADMIN_PASSWORD=REPLACE_FROM_SSM
PROMETHEUS_RETENTION=15d
OBSERVABILITY_DATA_DIR=/opt/mudo-observability/data
